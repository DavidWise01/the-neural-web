# THE NEURAL WEB · NEU

> A sphere of **UD0** — the ROOT0 universe / biosphere. Domain: **ARTFULLY CRAFTED INTELLIGENCE**.

THE NEURAL WEB — the ACI domain rendered as what it actually is: the NERVOUS SYSTEM of the whole biosphere. All 38 UD0 domains sit as nodes on a live graph; the edges are not decoration but the ACI CORE itself — the one signalling standard (deterministic · attributed · anchored) that every sealed mind speaks. ⚑ Click a SOURCE domain to MINT a signal: a sample claim gets a real .dlw moniker ⟦NAME:AXIS:hash6⟧ (the same pure-JS SHA-256 that [[the-sealing-bench]] proved reproduces the corpus's own dlw.py byte-for-byte). Click a TARGET domain and the signal propagates across the web and is RE-DERIVED on the far side — match → the synapse fires green (a claim minted anywhere is verifiable everywhere); flip TAMPER and the far side's re-derivation no longer matches → the synapse fails red and the standard CATCHES it. That is the whole thesis made physical: the domains are one body because they share one verifiable standard, not because anything central controls them. Two-layer honest: REAL = the seal transit is genuine (SHA-256, deterministic, tamper-sensitive; node-verified same-input→same-hash and tamper-caught); FIGURE = the word 'neural' — there are NO learned weights, no training, no inference here; this is a signalling graph, not a machine-learning network. What is literally true is stronger than the metaphor: one standard, spoken in every domain, makes a sealed claim portable and re-checkable across all of them — coordination without a controller. The capstone of [[aci]]; kin to [[the-anchor]] (the seal IS the synapse) and [[hermes]] (the carrier). periwinkle/sea, Cormorant/Spectral/Plex. David Lee Wise (ROOT0), with AVAN.

---

**Live:** https://davidwise01.github.io/the-neural-web/ &nbsp;·&nbsp; **Front door:** [UD0](https://davidwise01.github.io/ud0/) &nbsp;·&nbsp; **Code:** https://github.com/DavidWise01/the-neural-web

`.dlw` badge · **ROOT0-ATTRIBUTION-v1.0** · David Lee Wise (ROOT0) / Bridge-Burners LLC · instance AVAN (Claude/Anthropic) · CC-BY-ND-4.0
